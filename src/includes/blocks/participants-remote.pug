include ../mixins/participant

.participants
    .container
        h1 Remote Participants

        p This is the current list of remote attendees (who will participate via Google Hangouts)

        .row
            | {% for page in site.pages %}
            | {%   if page.title and page.type == 'participant-remote' %}
            .col-xs-4.col-sm-2
                +participant()
            | {%   endif  %}
            | {% endfor %}